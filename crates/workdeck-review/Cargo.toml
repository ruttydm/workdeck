[package]
name = "workdeck-review"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Review navigation and session state for Workdeck"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
workdeck-core = { path = "../workdeck-core" }
