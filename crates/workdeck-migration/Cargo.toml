[package]
name = "workdeck-migration"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "One-time Hunk-to-Workdeck configuration migration"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
toml = "0.8"

[dev-dependencies]
tempfile = "3.13"
