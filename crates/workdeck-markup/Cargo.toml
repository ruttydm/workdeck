[package]
name = "workdeck-markup"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Terminal-safe STML parser and renderer for Workdeck notes"

[dependencies]
quick-xml = "0.41"
serde = { version = "1.0", features = ["derive"] }
unicode-width = "0.2"
