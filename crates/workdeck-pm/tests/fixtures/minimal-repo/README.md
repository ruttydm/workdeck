# Minimal schema fixture

A valid schema-1 planning source and one issue. `auth-tests` and `issue` are
explicit declarative future verification references: loading is supported;
completion fails closed until their required verification behavior exists.
