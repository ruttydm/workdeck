[package]
name = "workdeck-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Durable local state and repository entities for Workdeck"

[dependencies]
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.13"
