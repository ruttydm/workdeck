name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  preflight:
    name: Semantic-port release gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Refresh namespaced Hunk refs
        run: cargo xtask port fetch
      - name: Verify complete Hunk ledger
        run: cargo xtask port audit
      - name: Formatting
        run: cargo fmt --all --check
      - name: Clippy
        run: cargo clippy --locked --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo xtask test
      - name: Dependency license policy
        uses: EmbarkStudios/cargo-deny-action@v2
      - name: Dependency security audit
        uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  build:
    name: Build ${{ matrix.target }}
    needs: preflight
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            executable: workdeck
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            executable: workdeck
          - os: macos-15-intel
            target: x86_64-apple-darwin
            executable: workdeck
          - os: macos-15
            target: aarch64-apple-darwin
            executable: workdeck
          - os: windows-2025
            target: x86_64-pc-windows-msvc
            executable: workdeck.exe
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
      - name: Verify native compiler host
        run: cargo xtask ci-host ${{ matrix.target }}
      - name: Build sole executable
        run: cargo build --locked --release --target ${{ matrix.target }} --package workdeck-cli --bin workdeck
      - name: Attest binary before packaging
        id: binary-provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: target/${{ matrix.target }}/release/${{ matrix.executable }}
      - name: Package licenses, SBOM, binary provenance, and checksum
        env:
          GH_TOKEN: ${{ github.token }}
        run: cargo xtask release package --target ${{ matrix.target }} --provenance "${{ steps.binary-provenance.outputs.bundle-path }}" --verify-ci
      - name: Attest archive provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist/workdeck-${{ matrix.target }}.*
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: workdeck-${{ matrix.target }}
          path: dist/workdeck-${{ matrix.target }}.*
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
