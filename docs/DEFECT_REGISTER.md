# Workdeck Defect Register

| Severity | Reproduction | Root fix | Regression | State |
| --- | --- | --- | --- | --- |
| P1 | Minimum-width Git displayed navigator and inspector over the same content | inspector is excluded while the navigator drawer is present | Playwright minimum-layout assertion and matrix | Closed |
| P1 | Empty first launch retained irrelevant navigator/inspector chrome | portfolio panes require a non-empty catalog | empty-fixture DOM assertion and snapshots | Closed |
| P1 | Native navigation rejected GitHub actions and did not explicitly admit loopback previews | allow exact loopback in-app; open only validated `https://github.com` externally | desktop navigation-policy test | Closed |
| P2 | An EmptyState could render a Retry button with no callback | removed action rendering from passive EmptyState; Git owns a real retry handler | all-target compile and Git error contract | Closed |
| P2 | Review’s canonical structure replaced content or lived in a generic inspector | dedicated level-indented canonical pane immediately right of content; global inspector suppressed for Review | responsive review snapshots and Axe | Closed |
| P2 | Screenshot updates silently reused a stale WebAssembly baseline | added destination-readiness assertions, tightened the image threshold, and forced snapshot regeneration from the current build | 76-state visual matrix | Closed |
| P1 | Packaged and web builds requested Geist from nested paths that Dioxus 0.7 flattens, producing font 404s | use `/assets/Geist.ttf` and `/assets/GeistMono.ttf`; package and verify both flattened resources | renderer CSS assertion, package resource gate, Playwright request log | Closed |
| P1 | Closing the primary Dioxus window could leave a running app with no reliably activatable window | keep the process alive with `WindowHides`, reveal the primary NSWindow on reopen, and move it to the active Space | desktop lifecycle tests plus exact-package close/reopen Computer Use flow | Closed |
| P2 | Inbox summary omitted the Suggested state and overemphasized raw work | expose now/waiting/suggested/snoozed with calm bounded groups and semantic next actions | fixture assertions and light/dark visual matrix | Closed |
| P2 | Native minimum-width QA depended on unreliable coordinate dragging | add an explicit `WORKDECK_WINDOW_PROFILE=minimum` launch profile that selects the product minimum while retaining the same signed executable | desktop size-profile test and exact-package 900×600 captures | Closed |
| P1 | Pull Requests and CI silently queried the portfolio's alphabetically first GitHub repository, so active projects such as SampleApp appeared empty | prioritize Inbox project providers, expose a persistent repository selector, and filter every provider request and fixture response by that explicit context | Rust ordering/deduplication test, Playwright cross-surface selector test, and exact-package PR/CI Computer Use flow | Closed |
| P1 | PR and commit rows looked selectable but several did not open stable task content | make every PR, commit, and Git-reference row a semantic button; open durable master/detail content keyed by repository and object identity | Playwright pointer/keyboard selection, mark-read stability, parent navigation, and reference-jump tests | Closed |
| P2 | Commits and Pull requests competed as separate global destinations despite being one repository activity workflow | keep one Git rail icon and use a compact titlebar tablist for Commits and Pull requests | rail-count, selected-tab, direct-shortcut, and responsive interaction tests | Closed |
| P1 | PR and commit review required extra navigation and scattered task context across summary, inspector, and reference panes | use one direct, minimal three-column surface: activity list, diff, and changed-files tree; auto-open the first task and move references to a popover | Playwright direct-selection, three-column geometry, tree-collapse, resize, keyboard, Axe, and 76-state visual matrix | Closed |
| P2 | Task sidebars consumed fixed space and could not be resized or collapsed | add persisted bounded pane widths, pointer/keyboard splitters, double-click reset, and task-owned collapse/reveal controls | Playwright task-pane and portfolio-pane resize/collapse matrix plus protocol bounds tests | Closed |
| P2 | Opening an unread PR could immediately move its row after the local read cursor advanced | retain the selected unread-origin row in its current group until selection changes | Rust grouping regression and Playwright selected-row stability test | Closed |
| P1 | Returning to Commits repeatedly displayed “Loading commit graph” and repeated Git/provider work; selecting a prepared commit or PR then loaded the same review a second time | add a bounded cross-target response cache with in-flight coalescing, prewarm the first available unread worktree, and pass prepared immutable change sets directly into the diff surface | 13 `workdeck-api` cache/protocol tests plus Playwright warm Git/PR revisit regression | Closed |
| P1 | The first response-cache implementation used `std::time::Instant`, which panicked in `wasm32-unknown-unknown` and left Git permanently on its skeleton | use pinned `web-time` as the monotonic native/browser clock and retain the wasm target plus browser interaction gates | wasm locked check, targeted Playwright reproduction, and full browser matrix | Closed |
| P2 | Refresh cleared the selected commit and diff before the replacement graph arrived | retain the graph, selection, and task content as stale-while-revalidate state; invalidate only the matching cache identity and surface a non-destructive refresh error | Playwright refresh-preservation regression and generation-rejection Rust test | Closed |
| P1 | The native macOS traffic lights first overlapped Workdeck's duplicate rail mark, then remained visually split by the rail boundary | remove the redundant mark, span one unified titlebar across the complete window, begin the rail below it, and keep an 80px native-control gutter at every responsive width | Playwright minimum-window shell-geometry regression plus source UI contract | Closed |
| P1 | Code review used 11px plain text, fixture diffs carried no spans, split rows had inconsistent cells, and the highlighter guessed a small set of tree-sitter node names | use official grammar highlight queries with a cached normalization pipeline; add 19 parser-backed languages, full semantic light/dark tokens, 12px/21px Geist Mono, sticky line gutters, honest language labels, and structurally identical unified/split/source rows | grammar-query/polyglot/UTF-8 Rust tests, fixture-span and renderer tests, Playwright typography/semantic/split regression, Axe, and responsive visual matrix | Closed |

Release status: there are no known open P0/P1 defects and no unexplained P2 defects. The current
three-column task package passes the complete deterministic source, browser, security,
packaging, and signature gates. Its exact-package Codex Computer Use rerun is still pending because
the official runtime stopped before inspection with `Computer Use requires nodeRepl.createElicitation`; the retained native
manifests are explicitly historical and bound to their older executable hash.
