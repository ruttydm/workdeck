id = "example.native-vcs"
name = "Native VCS example"
version = "0.1.0"
api_version = 1
executable = "bin/workdeck-example-native-vcs-extension"
capabilities = ["vcs-adapters"]
description = "Provider-neutral native VCS detection, loading, sources, and watch callbacks"
